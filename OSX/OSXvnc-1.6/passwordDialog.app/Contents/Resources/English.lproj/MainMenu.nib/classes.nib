{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {cancelButton = id; okButton = id; }; 
            CLASS = dialogController; 
            LANGUAGE = ObjC; 
            OUTLETS = {msgBox = NSTextField; responseBox = NSSecureTextField; window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}